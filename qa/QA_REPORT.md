# ABCD Restaurant — Astro Migration QA

## Migration result

- 23 clean static routes generated by Astro.
- Reservation runs as the only React island.
- Other interactive screens use scoped TypeScript client modules.
- Runtime assets are isolated under `public/assets/`.
- Historical QA artifacts and migration baselines are isolated under `reference/qa/` and excluded from production output.

## Validation gates

- `astro check`: 0 errors.
- Production build: passed.
- Generated-output link check: 23 HTML files, 0 broken local references.
- Playwright behavior suite: 30 passed.
- Playwright visual suite: 27 passed at a maximum 0.5% pixel-difference threshold.

Migration baselines were freshly captured from the committed legacy source because several supplied historical screenshots did not represent the final legacy HTML. The supplied artifacts remain unchanged under `reference/qa/screenshots/`; the immutable before-migration captures used by tests are under `reference/qa/migration-baselines/`.

## Deferred by scope

This migration intentionally does not add backend integration, persistent ordering state, production payment behavior, or frontend product improvements. Existing decorative controls and content remain unchanged.
